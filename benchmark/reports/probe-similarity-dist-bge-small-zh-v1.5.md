# 相似度分布与候选 floor 扫描（方案 §4.2）

> ⚠️ **本报告不产出结论，只产出候选。** 下面全部是 query-record pair 层面的
> 读数，而 §6.6 验收的是产品行为；中间隔着语义排名 → FTS 排名 → RRF → limit
> → semantic-only 限额，且同一条 empty query 可能有多条记录同时越过 floor。
> pair 通过率 5% 完全可以变成"每条 empty query 平均返回 1.3 条"。最终 floor
> 必须由每个候选各跑一次完整 `bun run bench` 的产品指标决定（§6.2）。

| 项 | 值 |
| --- | --- |
| 生成时间 | 2026-07-29T14:48:55.024Z |
| 命令行 | `bun run benchmark/probe-similarity-dist.ts --model=Xenova/bge-small-zh-v1.5 --label=bge-small-zh-v1.5 --json=/tmp/d2-bge-small-zh-v1.5-.json` |
| 编码器 | bge-small-zh-v1.5 |
| 模型 | `Xenova/bge-small-zh-v1.5` |
| 实测输出维度 | 512 |
| 前缀协议 | query=无 / doc=无 |
| 记录池 | primary 26 条 |
| query | relevance 36 / empty 5 |
| 当前生产 SEMANTIC_FLOOR | 0.2 |
| 运行环境 | Bun 1.2.20 / darwin arm64 |

## 三个池的分布

统计量用 p95/p99，不用 max：max 在 n=130 上极不稳定，样本一多只会往上走，
据此校准的 floor 会系统性偏低。max 仍然列出，但只作参考。

| 池 | 对数 | min | p5 | 中位数 | 均值 | p95 | p99 | max | 举证能力 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 正例（38 对） | 38 | 0.413 | 0.424 | 0.592 | 0.582 | 0.726 | 0.740 | 0.740 | 标注支撑 |
| 标注真负例（130 对） | 130 | 0.347 | 0.373 | 0.441 | 0.443 | 0.526 | 0.539 | 0.554 | 标注支撑，候选 floor 以其 p99 为准 |
| 未标注非目标对（898 对） | 898 | 0.192 | 0.340 | 0.469 | 0.465 | 0.578 | 0.608 | 0.650 | **有上偏**，仅供观察形状 |

真负例 p99 与未标注池 p99 的差值 = 0.069。它本身就是标注不完备程度的一个读数：未标注池里混着真正相关的记录，所以它系统性偏高。

### 分离度

**AUC 是本报告里唯一可以跨编码器横向比较的读数。** 它 = P(随机正例对 > 随机真负例对)，
平局记 0.5，只依赖两个分布的相对顺序，与该模型把分数压在哪个绝对区间无关。
0.5 = 完全无判别力，1.0 = 任何正例都高于任何真负例。

下面那张"对照"表里的绝对差值**不可横向比较**：句向量模型的各向异性差别很大，
E5 家族的 cosine 会全部挤在一个窄高带里，绝对间距小并不代表顺序分不开。用绝对
间距排序候选会得出与实际排名相反的结论。

| 分离度 | 值 |
| --- | --- |
| **AUC（正例 vs 标注真负例）** | **0.918** |
| AUC（正例 vs 未标注非目标对，有上偏） | 0.854 |

不做"正例 p5 > 真负例 p99"这类二元分离检验——那要求近乎完美分离，真实语料上
的句向量模型基本不可能过，写成判据等于预先把"删门"分支判死（v2 犯过这个错）。
以下只列几个**同一模型内部**可读的对照量。

| 对照 | 值 |
| --- | --- |
| 正例中位数 − 真负例中位数 | 0.152 |
| 正例 p5 − 真负例 p99 | -0.115 |
| 正例中位数 − 真负例 p99 | 0.053 |
| 真负例分布落在正例 [min, max] 区间内的比例 | 79.2% |

## 候选 floor 扫描

`positiveRecall@t` = 38 个正例对里 cosine > t 的比例（保住多少真召回）。
`trueNegRate@t` = 130 对标注真负例里 cosine > t 的比例（放进多少确定的噪声）。
`unlabeledRate@t` = 898 对未标注非目标对的同一比例（**有上偏**，仅参考）。

| t | positiveRecall@t | trueNegRate@t | unlabeledRate@t |
| --- | --- | --- | --- |
| 0.420 | 97.4% | 70.8% | 74.3% |
| 0.433 | 92.1% | 59.2% | 68.8% |
| 0.445 | 92.1% | 46.2% | 61.6% |
| 0.458 | 89.5% | 40.8% | 55.3% |
| 0.471 | 89.5% | 20.8% | 49.2% |
| 0.483 | 86.8% | 13.8% | 40.4% |
| 0.496 | 86.8% | 10.0% | 35.6% |
| 0.509 | 84.2% | 8.5% | 30.3% |
| 0.522 | 78.9% | 5.4% | 23.3% |
| 0.534 | 73.7% | 3.1% | 18.2% |
| 0.547 | 68.4% | 0.8% | 12.6% |
| 0.560 | 57.9% | 0.0% | 8.8% |
| 0.572 | 55.3% | 0.0% | 5.7% |
| 0.585 | 52.6% | 0.0% | 3.5% |
| 0.598 | 39.5% | 0.0% | 1.8% |
| 0.610 | 36.8% | 0.0% | 0.9% |
| 0.623 | 34.2% | 0.0% | 0.4% |
| 0.636 | 26.3% | 0.0% | 0.1% |
| 0.648 | 21.1% | 0.0% | 0.1% |
| 0.661 | 18.4% | 0.0% | 0.0% |
| 0.674 | 18.4% | 0.0% | 0.0% |
| 0.687 | 7.9% | 0.0% | 0.0% |
| 0.699 | 5.3% | 0.0% | 0.0% |
| 0.712 | 5.3% | 0.0% | 0.0% |
| 0.725 | 5.3% | 0.0% | 0.0% |
| 0.737 | 2.6% | 0.0% | 0.0% |
| 0.750 | 0.0% | 0.0% | 0.0% |

### 候选（启发式，不是判据）

规则写明：取真负例通过率**首次**跌破各档位的那个 t。选中哪个由完整 benchmark
的产品指标决定，不由这张表决定。

| 档位（trueNegRate ≤） | 候选 t | positiveRecall@t | trueNegRate@t | unlabeledRate@t |
| --- | --- | --- | --- | --- |
| 50.0% | **0.445** | 92.1% | 46.2% | 61.6% |
| 25.0% | **0.471** | 89.5% | 20.8% | 49.2% |
| 10.0% | **0.496** | 86.8% | 10.0% | 35.6% |
| 5.0% | **0.534** | 73.7% | 3.1% | 18.2% |
| 1.0% | **0.547** | 68.4% | 0.8% | 12.6% |

## 历史对照

旧口径（小样本取 max）：正例 0.080–0.607、噪声 max 0.431。保留作对照，
**不再用于任何决策**——`SEMANTIC_FLOOR = 0.2` 就是这么来的。
